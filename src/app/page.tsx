import {InventoryScreen} from '@/src/components/InventoryScreen';

export const metadata = {title: 'Inventory — Weldam House'};

export default function InventoryPage() {
  return <InventoryScreen />;
}
